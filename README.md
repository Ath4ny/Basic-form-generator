# Приложение для генерации форм

## Технологии:

- Vue.js 3 + Vue Router & Vuex
- Typescript
- Sass/SCSS

## Использование компонента генерации форм:

Компонент принимает два пропса config и model-value (Применяется через v-model)

> config - принимает структуру полей для формы (прим.1)

### Пример (1.) структуры

```
{
 id: 'home-form',
 title: 'Регистрация',
 description: 'Создайте новый аккаунт',
 fields: [
   {
     name: 'username',
     type: 'input',
     label: 'Имя пользователя',
     required: true,
     placeholder: 'Введите имя пользователя',
     attributes: { autocomplete: 'username' },
     validation: {
       minLength: 3,
       maxLength: 20,
     },
   },
 ]
 submitButton: {
   text: 'Зарегистрироваться',
 },
 cancelButton: {
   text: 'Сбросить',
 },
}
```

> model-value - принимает значения полей в виде объекта, данные соотносятся с конфигом по полю name для автозаполнения, потому если добавить данные в config, но не добавлять в объект model-value - то данные не смогут быть корректно валидированы

### Базовое применение

```
<FormGenerator
   v-model="formData"
   :config="formConfig"
   @submit="saveToStore"
   @cancel="deleteFromStore"
 ></FormGenerator>
```

При таком подходе будет осуществляться работа, если достаточно базовой структуры и нет необходимости в кастомных полях

### Применение со слотами

```
 <FormGenerator
    v-model="formData"
    :config="formConfig"
    @submit="saveToStore"
    @cancel="deleteFromStore"
  >
    <template #password="slotProps">
      <div class="password-field">
        <div class="input-group">
          <input
            :id="slotProps.field.name"
            :type="showPassword ? 'text' : 'password'"
            :value="slotProps.value"
            autocomplete="new-password"
            @input="slotProps.updateValue($event)"
            class="password-input"
            placeholder="Введите пароль"
          />
          <button type="button" @click="showPassword = !showPassword" class="password-toggle">
            {{ showPassword ? '👁️' : '👁️‍🗨️' }}
          </button>
        </div>
        <div class="password-strength">
          <div class="strength-bar">
            <div class="strength-fill" :class="passwordStrengthClass"></div>
          </div>
          <span class="strength-text">{{ passwordStrengthText }}</span>
        </div>
      </div>
    </template>
  </FormGenerator>
```

> Пример с кастомным полям для ввода пароля, который проверяет сложность введённого пароля. При этом при таком подходе всё также необходимо описывать поле в структуре (config)

## Особенности работы

Генератор форм работает используя компонент formField, который в свою очередь может работать как с базовыми инпутами, так и компонентами засчёт Вьюшного \<component>, где тип выбирается вычисляемым значением по объекту fieldTypeMaps, таким образом возможно легко импортировать и подключить необходимый для работы компонент, расширив данный объект

# Демо

Сейчас логика демо страниц выглядит так:

- Есть 3 страницы с различными вариантами форм и одна страница 404, для корректной работы (отображается при переходе на несуществующую страницу)
- На каждой из страниц с формами есть 2 кнопки:
  - Сохранить (Submit) - которая, при прохождении валидации сохраняет значение формы и добавляет их в стор
  - Сбросить (Cancel) - которая чистит значение полей и удаляет их из стора

- Для показательности работы форм добавлена простая валидация полей

# Нюансы реализациии

Приложение конечно же имеет свои уступки, на который пришлось пойти дабы не усложнять его излишними уровнями абстракций (всё чтобы оно оставалось в рамках демо), например:

- Логику стора можно было вытащить в composable
- В компоненте formField, есть намеренно добавленное разделение логики на checkbox и остальные компоненты, для сохранения стилистики (некоторый внутренний перфекционизм)
- Некоторую логику работы с данными в формах также возможно было вытащить в composables
- На страницах логика получения данных для форм идёт исключительно из стора, а данные в сторе не сохраняются в какой-нибудь localStorage, для долговременного хранения
- Нет консистентности в стилях scss, особенно в цветах

## Project Setup

```sh
npm install
```

### Compile and Hot-Reload for Development

```sh
npm run dev
```

### Type-Check, Compile and Minify for Production

```sh
npm run build
```
